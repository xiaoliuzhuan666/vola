apiVersion: apps/v1
kind: Deployment
metadata:
  name: vola-server
  namespace: vola
spec:
  replicas: 1
  selector:
    matchLabels:
      app: vola-server
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: vola-server
    spec:
      containers:
        - name: vola-server
          image: vola:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
          env:
            - name: PORT
              valueFrom:
                configMapKeyRef:
                  name: vola-config
                  key: PORT
            - name: CORS_ORIGINS
              valueFrom:
                configMapKeyRef:
                  name: vola-config
                  key: CORS_ORIGINS
            - name: PUBLIC_BASE_URL
              valueFrom:
                configMapKeyRef:
                  name: vola-config
                  key: PUBLIC_BASE_URL
            - name: GIT_MIRROR_HOSTED_ROOT
              valueFrom:
                configMapKeyRef:
                  name: vola-config
                  key: GIT_MIRROR_HOSTED_ROOT
            - name: INSTANCE_ADMIN_USER_IDS
              valueFrom:
                configMapKeyRef:
                  name: vola-config
                  key: INSTANCE_ADMIN_USER_IDS
            - name: VOLA_ENABLE_PUBLIC_REGISTRATION
              valueFrom:
                configMapKeyRef:
                  name: vola-config
                  key: VOLA_ENABLE_PUBLIC_REGISTRATION
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: vola-app
                  key: DATABASE_URL
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: vola-app
                  key: JWT_SECRET
            - name: VAULT_MASTER_KEY
              valueFrom:
                secretKeyRef:
                  name: vola-app
                  key: VAULT_MASTER_KEY
            - name: GITHUB_CLIENT_ID
              valueFrom:
                secretKeyRef:
                  name: vola-app
                  key: GITHUB_CLIENT_ID
            - name: GITHUB_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: vola-app
                  key: GITHUB_CLIENT_SECRET
            - name: GITHUB_APP_CLIENT_ID
              valueFrom:
                secretKeyRef:
                  name: vola-app
                  key: GITHUB_APP_CLIENT_ID
            - name: GITHUB_APP_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: vola-app
                  key: GITHUB_APP_CLIENT_SECRET
            - name: GITHUB_APP_SLUG
              valueFrom:
                secretKeyRef:
                  name: vola-app
                  key: GITHUB_APP_SLUG
          volumeMounts:
            - name: vola-git-mirrors
              mountPath: /data/git-mirrors
          livenessProbe:
            httpGet:
              path: /api/health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 6
          readinessProbe:
            httpGet:
              path: /api/health
              port: 8080
            initialDelaySeconds: 3
            periodSeconds: 5
            timeoutSeconds: 5
            failureThreshold: 6
      volumes:
        - name: vola-git-mirrors
          persistentVolumeClaim:
            claimName: vola-git-mirrors
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vola-git-mirrors
  namespace: vola
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
---
apiVersion: v1
kind: Service
metadata:
  name: vola-server
  namespace: vola
spec:
  selector:
    app: vola-server
  ports:
    - port: 8080
      targetPort: 8080
