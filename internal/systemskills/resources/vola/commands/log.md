# `vola log`

Use this command when the user wants to append a structured project log entry.

## Example

- `vola log project/demo --action note --summary ./summary.md`

## Notes

- The summary may be literal text, stdin, or a local file path.
- `log` currently targets `project/<name>`.
