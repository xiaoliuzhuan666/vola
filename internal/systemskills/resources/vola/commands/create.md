# `vola create`

Use this command when the user wants to create a first-class Vola object.

## Current Public Form

- `vola create project <name>`

## Notes

- Keep the category after the verb.
- A leading `/` is optional if the user writes `/project`.
