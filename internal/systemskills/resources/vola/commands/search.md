# `vola search`

Use this command when the user wants to search Vola content.

## Examples

- `vola search "migration"`
- `vola search "memory marker" memory`
- `vola search "project note" project/demo`

## Notes

- A leading `/` is optional on the scope path.
- `secret` search is not part of the public command surface.
